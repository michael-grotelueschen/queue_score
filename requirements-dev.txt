# Development dependencies: runtime + test suite + notebook regeneration tooling.
# Install with:  .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.0

# Needed only to regenerate the notebooks/ via their build_*.py scripts.
nbformat==5.10.4
nbconvert==7.17.1
ipykernel==7.3.0
