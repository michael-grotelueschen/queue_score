# Runtime dependencies for the model pipeline (model/train.py) and data fetch.
# Pinned to known-good versions; SHAP and scikit-learn in particular are
# version-sensitive (SHAP output shape, sklearn APIs). Install with:
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
numpy==2.2.6
pandas==2.3.3
openpyxl==3.1.5          # pandas .xlsx engine for the LBNL workbooks
scikit-learn==1.7.2
shap==0.49.1             # exact TreeExplainer contributions for the random forest
matplotlib==3.10.9
certifi==2026.5.20       # CA bundle for data/fetch_workbooks.py downloads
